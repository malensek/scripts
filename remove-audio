#!/usr/bin/env bash
################################################################################
# remove-audio - removes all audio tracks from a video file using ffmpeg.
################################################################################

ffmpeg -i "${1}" -vcodec copy -an "${2}"
